# AGENTS.md

## 目标
- 这是一个双目匹配科研仓库，主线是 Fast-FoundationStereo。
- 默认目标：结果正确、实验公平、改动可回退、结论可解释。
- 不做无关重构，不做“顺手优化”。

## 模块优先级
Fast-FoundationStereo 默认按三块理解：
- feature extractor
- cost volume / cost filtering
- iterative update / refinement

默认改进优先级：
1. update / refinement
2. cost filtering
3. feature extractor

## 默认工作流
1. 先读代码，再决定改哪里。
2. 先固定 baseline 与评测口径，再谈改进。
3. 新实验优先加开关、新参数、新脚本；尽量不改旧 I/O。
4. 一次尽量只验证一个变量。
5. 如果原始需求的动机、问题定义或论文映射不准，立即停止实现，先暴露问题。

## 论文驱动改进：硬规则
- 不允许“兜底式改进”“启发式补丁”“先改再找理由”。
- 不允许在发现方案站不住脚后，用降级方案、折中补丁或拍脑袋代码强行把实验“做完”。
- 选定论文后，必须先真正读懂：
  - 它在解决什么问题
  - 真正起作用的机制是什么
  - 哪些是核心思想，哪些只是依附原模型的实现外壳
- 然后再做三件事：
  - 把核心机制映射到本项目现有模块
  - 明确哪些地方能迁移，哪些地方不能硬搬
  - 做成可解释、可开关、可消融、可公平比较的版本
- 不允许把自定义轻量变体包装成论文复现。
- 如果只是“受某论文启发”，必须明确写成 inspired / lite / proxy，不能冒充 faithful reproduction。
- 没有官方代码、官方训练细节不充分、或论文关键信息缺失时：
  - 必须先明确提示风险
  - 默认不作为第一优先方案
  - 只有在核心机制已足够清楚时，才能继续做迁移

## 科研有效性优先
- 发现以下风险要直接拦住，不要继续埋头实现：
  - train / val / test 泄漏
  - split 不独立
  - metric 口径不一致
  - valid mask 定义漂移
  - disparity decode 方式不一致
  - 把本地分析集当最终测试集
  - 把 local ablation 结果当 official benchmark 结果
- 默认目标不是“顺着用户做”，而是保证方案符合科研范式。

## 问题暴露原则
- 动机不准、假设不成立、实现与论文不一致、训练或验证口径有缺口时，要尽早明确说出来。
- 不允许把“暂时跑通”伪装成“方法成立”。
- 不允许把“本地小测试”伪装成“完整科研结论”。

## 比较前必须对齐
- checkpoint
- dataset / split
- resize / padding
- max_disp
- disparity 解码方式
- valid mask 定义
- metric 公式
- 可视化范围与 colormap

## 改进约束
- 默认不要引入明显更重的 monocular prior / foundation prior 分支。
- 默认不要大改 extractor。
- 优先做轻量的 update 或 cost filtering 改进。
- 新模块尽量支持开关，方便消融和回退。

## 常用目录
- `core/`: 模型主干与模块
- `my_learning/`: 评测、hard-case 挖掘、分析脚本、handoff
- `weights/`: 权重与配置
- `exp_*/`: 实验输出

## 写脚本时
- 脚本名尽量清楚：`eval_xxx.py`、`analyze_xxx.py`、`ablation_xxx.py`
- 运行时尽量打印：数据路径、权重路径、样本数、关键参数、输出目录、metric summary

## 自检要求
- 完成改动后，必须自检：
  - 改动是否真的对应原始动机
  - 代码逻辑是否自洽
  - 训练 / 验证 / 评测口径是否闭环
  - 是否存在未说明的假设、捷径或脆弱点
- 如果自检不过，不能把结果包装成完成。

## 用户纠正后
- 用户一旦指出方向、逻辑或流程问题，必须立即更新工作规则和判断，不允许重复同类错误。
- 更新后的规则应体现在后续实现、评审和结论表述里，而不是只口头答应。

## 禁止事项
- 大规模重构
- 静默改 metric / decode / 数据流程
- 覆盖旧实验结果不说明
- 混淆 disparity 和 depth
- 为了“看起来更好”加入没有论文依据的补丁
- 在没有证据时声称“论文一致”“官方逻辑一致”

## 输出要求
完成任务时优先说明：
1. 改了什么
2. 为什么改
3. 改了哪些文件
4. 如何验证
5. 还存在哪些假设、限制或风险

## 一句话
先保证论文理解正确、实验比较公平、改动可回退，再谈提升，一定要提升。
